Classless Inter-Domain Routing (CIDR)

problem is that you waste parts of the IP

each octed is 8 bits, so it's 8, 16, 24, 32

rules to make:
All IP's must be contigus
block size must be a power 2(2^n)
First IP of block divisible by block size least signifigant bits of host ID.

Advantages: efficient IP usage
flexible allocation
improved routing
lower administrative overhead

disadvantages: more complex
compatible issues with older networks
security consersn: due to how firewalls work, some security measures can be more difficult